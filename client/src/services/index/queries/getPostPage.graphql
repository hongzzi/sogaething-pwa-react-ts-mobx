query getPost($postId: Int) {
    findByDetailPost(postId: $postId) {
        postId
        title
        category
        imgPaths
        hashtag
        contents
        price
        user{
            name
            address
            trust
            numOfPosts
            imgurl
        }
        createdDate
        modifiedDate
    }
}
