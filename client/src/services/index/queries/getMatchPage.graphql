mutation createMatching($input: MatchInput!) {
    createMatching(input: $input) {
        matchingId
        user {
                userId
                name
                imageUrl
                trust
        }
        category
        minPrice
        maxPrice
        transaction
        createdDate
        modifiedDate
    }
}