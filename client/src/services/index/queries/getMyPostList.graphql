query getMyPosts($userId: Int){
    findPostListByUserId(userId: $userId) {
        postId
        title
        category
        imgPath
        price
        hashtag
        createdDate
        modifiedDate
    }
}